{
  "tasks": {
    "serve": "deno run --allow-net --allow-read --allow-env src/server.ts",
    "test": "deno test --allow-env",
    "cache:reload": "deno cache --reload --lock=deno.lock src/deps.ts"
  }
}
